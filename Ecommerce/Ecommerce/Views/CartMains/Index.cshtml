@model IEnumerable<Ecommerce.Models.CartMain>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeliveryDetails.billing_first_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cart_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Discount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeliveryDetails.billing_first_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cart_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Discount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cart_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cart_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cart_id })
        </td>
    </tr>
}

</table>
